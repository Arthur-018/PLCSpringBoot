# Frontend (React + Vite)

Este diretório contém o frontend em React (JavaScript) para integrar com o backend Spring Boot.

## Pré-requisitos

- Node.js 18+

## Comandos

```bash
npm install
npm run dev
```

A aplicação ficará disponível em `http://localhost:5173`.
